#include <stdio.h>
#include <string.h>

int mian(){

    int len;
    int intoctal;
    char octal[100];
    printf("Enter Num in Octal :  ");
    scanf("%s", octal);
    len = strlen(octal);
    intoctal = atoi(octal);











}